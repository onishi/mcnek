# 道の駅一覧 Web サービス計画

道の駅を探す、地図で見る、自分が行った駅を記録するための Web サービスを作る。

## 使うデータソース

まずは次の 2 種類を使う。

- 国土交通省 道の駅一覧
  - https://www.mlit.go.jp/road/Michi-no-Eki/list.html
  - 公式の登録情報として扱う
  - 駅名、登録回、登録年月、所在地の一覧を取得する
  - 2025-12-19 時点で 1,231 駅が登録されている
- 全国「道の駅」連絡会 道の駅検索
  - https://www.michi-no-eki.jp/stations/search/all/all/all
  - 詳細情報の補完元として扱う
  - 都道府県、市区町村、個別ページ、検索ページの情報を確認する

国土交通省の一覧をマスターデータにし、全国「道の駅」連絡会の情報を詳細情報として突き合わせる。両方に差分がある場合は、まず国土交通省の登録情報を正とし、差分を確認できる管理用レポートを出す。

## ローカル起動手順

```sh
npm install
npm run dev
```

- ブラウザで `http://localhost:5173/` を開く
- `npm test`: ユニットテスト・コンポーネントテストを実行する
- `npm run lint`: ESLint でコードをチェックする
- `npm run format`: Prettier でコードを整形する
- `npm run format:check`: Prettier の整形チェックのみ行う（CI 向け）
- `npm run build`: 型チェックと本番ビルドを実行する

## データ更新手順

公式データを最新化するときは、以下を順番に実行する。

```sh
npm run import:fetch            # 国土交通省 XLS を取得（data/raw/ に保存、Git管理外）
npm run import:mlit             # XLS を正規化し src/data/generated/mlitStations.json を更新
npm run import:michinoeki:fetch # 連絡会サイトをスクレイピング（data/raw/ に保存）
npm run import:michinoeki:merge # 連絡会データを突き合わせ、手動紐付け（data/manual/）も適用
```

- `npm run import:mlit` を実行すると、前回コミットされていたデータとの差分（新規登録・廃止・名称変更・所在地変更）を `src/data/generated/mlitStationsDiff.json` に保存し、コンソールにも件数を表示する
- 取得日時とデータ件数は `src/data/generated/importMeta.json` に保存される
- `npm run dev` で `/data-check` を開くと、取得状況と差分レポートをまとめて確認できる
- 差分の内容を確認したうえで、生成された JSON ファイルをコミットする
- 自動で突き合わせできなかった連絡会データは `/data-check` の手動紐付け UI でチェックし、「JSONでコピー」した内容を `data/manual/michiNoEkiManualLinks.json` に保存してから `import:michinoeki:merge` を再実行する

## 開発フェーズ

小さく作って、完了したらチェックを付けて進める。各フェーズは原則 1 つの PR で終わる大きさにする。

### 0. プロジェクト土台

- [x] TypeScript / React のプロジェクトを作る
- [x] TypeScript strict モードを有効にする
- [x] Jest を導入する
- [x] lint / format / test の npm scripts を用意する
- [x] 空のトップページを表示できるようにする
- [x] README にローカル起動手順を書く

完了条件:

- `npm test` が実行できる
- ローカルで空のアプリが起動する

### 1. 道の駅データ型を決める

- [x] `RoadsideStation` 型を作る
- [x] `VisitRecord` 型を作る
- [x] 都道府県、地方、登録年月の扱いを決める
- [x] サンプル道の駅データを 3 件だけ作る
- [x] 型とサンプルデータのユニットテストを作る

完了条件:

- UI や取り込み処理が依存する最小データ構造が決まっている
- サンプルデータだけで一覧画面を作り始められる

### 2. サンプルデータ一覧

- [x] サンプルデータを一覧表示する
- [x] 駅名、都道府県、所在地を表示する
- [x] 一覧の件数を表示する
- [x] スマートフォン幅でも読めるレイアウトにする
- [x] 一覧表示のコンポーネントテストを作る

完了条件:

- まだ実データでなくても、道の駅一覧サービスとして最初の画面が見える

### 3. 検索と絞り込み

- [x] 駅名検索を追加する
- [x] 都道府県フィルターを追加する
- [x] 地方フィルターを追加する
- [x] 検索結果が 0 件の表示を作る
- [x] 検索・絞り込み関数のユニットテストを作る

完了条件:

- サンプルデータに対して検索と絞り込みができる
- データ件数が増えても UI 側の基本構造を変えずに済む

### 4. 詳細ページ

- [x] 駅ごとの詳細 URL を作る
- [x] 詳細ページに駅名、所在地、登録年月を表示する
- [x] 一覧から詳細ページへ移動できる
- [x] 存在しない駅 ID の表示を作る
- [x] 詳細ページのコンポーネントテストを作る

完了条件:

- サンプルデータの各駅を個別ページとして見られる

### 5. 国土交通省データ取り込みの最小実装

- [x] 国土交通省一覧ページから XLS の取得 URL を特定する
- [x] XLS をローカルに保存する取得スクリプトを作る
- [x] XLS から駅名、登録回、登録年月、所在地を読み取る
- [x] 読み取ったデータを JSON に出力する
- [x] 取り込み件数をログに出す
- [x] XLS パーサーのユニットテストを作る

完了条件:

- 国土交通省の公式一覧から JSON を生成できる
- 生成された JSON をアプリで読める

### 6. 国土交通省データの正規化

- [x] 所在地から都道府県を取り出す
- [x] 所在地から市区町村を取り出す
- [x] 登録年月を扱いやすい文字列に揃える
- [x] 駅ごとの安定した `id` を生成する
- [x] 重複 ID を検出する
- [x] 正規化関数のユニットテストを作る

完了条件:

- 国土交通省データを一覧・検索でそのまま使える形にできる

### 7. 実データ一覧への切り替え

- [x] サンプルデータから生成 JSON に切り替える
- [x] 一覧に全駅を表示する
- [x] 都道府県別の件数を表示する
- [x] 検索と絞り込みを実データで確認する
- [x] 表示件数が多くても操作しやすい UI に調整する

完了条件:

- 全国の道の駅を一覧で探せる MVP になる

### 8. 全国「道の駅」連絡会データの補完

- [x] 検索ページのページング構造を確認する
- [x] 検索結果から駅名、都道府県、市区町村、詳細 URL を取得する
- [x] 国土交通省データと駅名・所在地で突き合わせる
- [x] 突き合わせできなかった駅をレポートに出す
- [x] 補完データを JSON に保存する
- [x] 突き合わせ処理のユニットテストを作る
- [x] 突き合わせ状況を確認できる管理画面（`/data-check`）を作る
- [x] 国土交通省：連絡会を 1 対多で表せるようにする（上り線・下り線などの分割に対応）
- [x] 自動マッチングできない駅を管理画面から手動で紐付けられるようにする

完了条件:

- 国土交通省データを壊さずに、連絡会サイトの詳細 URL を持てる
- 自動で突き合わせできない駅も、手動で紐付けて解消できる

### 9. 外部リンク表示

- [x] 詳細ページに国土交通省の参照リンクを表示する
- [x] 詳細ページに全国「道の駅」連絡会の詳細リンクを表示する
- [x] リンクがない場合の表示を決める
- [x] 外部リンク表示のテストを作る

完了条件:

- ユーザーが公式情報へ辿れる

### 10. 地図用データ準備

- [x] 緯度経度の取得元を決める
- [x] 緯度経度を `RoadsideStation` に保存する
- [x] 緯度経度がない駅を検出する
- [x] 緯度経度なしでも一覧・詳細が壊れないようにする
- [x] 緯度経度データのバリデーションテストを作る

完了条件:

- 地図表示に必要なデータの欠落状況がわかる

### 11. 地図 MVP

- [ ] 連絡会サイトの詳細ページから番地まで含む住所を取得する（国土交通省データの「所在地」は市区町村単位までのため、そのまま座標化すると約半数の駅で座標が重複する。詳細は技術メモ参照）
- [ ] 取得した住所を GSI 住所検索 API で座標化し `RoadsideStation` に保存する
- [ ] 地図ライブラリを選ぶ
- [ ] 全国地図を表示する
- [ ] 緯度経度がある駅だけピン表示する
- [ ] ピンから詳細ページへ移動できる
- [ ] 地図がモバイル幅で使えることを確認する

完了条件:

- 地図から道の駅を探せる最小機能がある

### 12. 行ったリストのローカル保存

- [x] 詳細ページに「行った」切り替えを追加する
- [x] 訪問済み状態を localStorage に保存する
- [x] 一覧で訪問済み状態を表示する
- [x] 訪問済みだけの絞り込みを追加する
- [x] localStorage の読み書き関数をテストする

完了条件:

- ログインなしで、自分が行った道の駅を記録できる

### 13. 行ったリストの見える化

- [x] 行った駅だけの一覧ページを作る
- [x] 都道府県別の訪問数を表示する
- [x] 全体の訪問数と達成率を表示する
- [ ] 地図上で訪問済みピンを区別する（フェーズ11の地図 MVP が前提のため未着手）
- [x] 訪問数集計関数のユニットテストを作る

完了条件:

- 自分の訪問状況を一覧と地図で確認できる

### 14. データ更新運用

- [x] データ取得コマンドを npm script にする（`import:fetch` / `import:mlit` / `import:michinoeki:fetch` / `import:michinoeki:merge`）
- [x] 取得日とデータ件数を保存する
- [x] 前回データとの差分を出す
- [x] 新規登録、名称変更、所在地変更を分けて表示する
- [x] 差分レポートを `/data-check` に統合し、更新ごとの確認を1画面で完結させる
- [x] データ更新手順を README または docs に書く

完了条件:

- 最新データへの更新を安全にレビューできる

### 15. 初期リリース前の仕上げ

- [ ] README の完了済みフェーズにチェックを入れる
- [ ] 基本画面の表示崩れを確認する
- [ ] 主要導線の手動確認手順を書く
- [ ] テストカバレッジを確認する
- [ ] 未実装機能を issues または README に整理する

完了条件:

- 一覧、詳細、地図、行ったリストの最小機能を公開できる

---

ここから先は初期リリース後の発展フェーズ。優先度や内容は実際の利用状況を見ながら見直す。

### 16. 連絡会データ突き合わせの精度向上

- [x] 残っている未紐付け駅を `/data-check` の手動紐付け UI で解消する（2026-06-24 時点で 1,231/1,231 件が一致）
- [x] 括弧の種類（「」『』）や中点（・･）など表記揺れの正規化パターンを増やす
- [ ] 自動マッチング候補をスコア付きで提示する（手動紐付けの候補選びを楽にする）。現時点では未紐付け駅が無いため保留中。今後データ更新で新たに未紐付けが発生した際に再検討する
- [x] 手動紐付けデータ（`data/manual/michiNoEkiManualLinks.json`）の妥当性チェック（存在しない駅 ID やパスを検出する）

完了条件:

- 国土交通省データの大部分が連絡会データと紐付いている状態を維持できる

### 17. PWA 化・オフライン対応

- [ ] Service Worker を導入し、一度開いた一覧・詳細をオフラインでも表示できるようにする
- [ ] ホーム画面に追加できるようにする（manifest 設定）
- [ ] オフライン時の表示（データが古い可能性がある旨など）を用意する
- [ ] オフライン対応のテスト方針を決める

完了条件:

- 電波が不安定な場所でも、直前まで見ていた道の駅情報を確認できる

### 18. パフォーマンス・アクセシビリティ改善

- [ ] 1,200 件超の一覧表示のレンダリング負荷を見直す（リスト仮想化など）
- [ ] Lighthouse でパフォーマンス・アクセシビリティスコアを計測する
- [ ] キーボード操作・スクリーンリーダーでの主要導線を確認する
- [ ] 画像（地図ピン・アイコンなど）の最適化を行う

完了条件:

- モバイル回線・低スペック端末でも一覧・地図が問題なく操作できる

### 19. 公開・運用準備

- [ ] 本番ホスティング先を決める（Vercel / Cloudflare Pages など）
- [ ] CI で lint / test / build を自動実行する
- [ ] 独自ドメインを用意する（任意）
- [ ] 簡易的なアクセス解析を導入する（任意）
- [ ] 障害時の切り戻し手順を決める

完了条件:

- 誰でもアクセスできる本番環境で安定して動作している

### 20. 多言語対応の検討

- [ ] 訪日外国人向けに英語表記が必要かを検討する
- [ ] 対応する場合、駅名・所在地の英語表記の取得元を決める
- [ ] 言語切り替え UI を用意する
- [ ] 英語表記がない駅の扱いを決める

完了条件:

- やる場合は、英語でも一覧・詳細・地図を使える

## データモデル

`src/types/roadsideStation.ts` で定義。

```ts
type Prefecture = "北海道" | "青森県" | ... ; // 47 都道府県のリテラル型
type Region =
  | "北海道地方"
  | "東北地方"
  | "関東地方"
  | "中部地方"
  | "近畿地方"
  | "中国地方"
  | "四国地方"
  | "九州・沖縄地方";

type RoadsideStation = {
  id: string;
  name: string;
  prefecture: Prefecture;
  municipality: string;
  address: string;
  registrationRound: number | null;
  registrationDate: string | null; // "YYYY-MM" 形式
  latitude: number | null;
  longitude: number | null;
  mlitSourceUrl: string;
  associationSourceUrls: string[]; // 連絡会データは上り線・下り線などで1対多になる場合がある
  updatedAt: string;
};

type VisitRecord = {
  stationId: string;
  visitedAt: string | null;
  memo: string | null;
  createdAt: string;
  updatedAt: string;
};
```

- 都道府県は 47 件のリテラル型 `Prefecture` で表す（`src/types/roadsideStation.ts`）
- 地方（`Region`）は `RoadsideStation` のフィールドとして持たず、`prefecture` から `getRegion()`（`src/data/regions.ts`）で導出する。データの重複・不整合を避けるため
- 登録年月は `"YYYY-MM"` 形式の文字列、または未登録時は `null`
- サンプルデータ 3 件は `src/data/sampleStations.ts` にある

## 優先しないこと

初期リリースでは、次の機能は後回しにする。

- 口コミ、評価、ランキング
- 写真アップロード
- 複数ユーザーのフォロー
- スタンプラリーの細かいルール対応
- 経路検索
- 営業時間や休業日の完全な自動更新

まずは「公式データを元に全国の道の駅を探せる」「地図で見られる」「自分が行った駅を記録できる」状態を優先する。

## 技術メモ

- TypeScript strict モードで実装する
- React は関数コンポーネントを基本にする
- 新規関数には Jest のユニットテストを追加する
- データ取り込み、正規化、差分検出は UI と分離する
- ルーティングは react-router-dom を使う（詳細ページの URL は `/stations/:id`）
- 地図ライブラリは実装時に選定する
- 緯度経度の取得元と利用条件は実装前に確認する
- 国土交通省データの XLS 取得 URL は `https://www.mlit.go.jp/road/Michi-no-Eki/file/list.xls`
- 国土交通省データの「所在地」列は市区町村までで、番地を含む完全な住所ではない。完全な住所はフェーズ 8（全国「道の駅」連絡会データ）で補完する想定
- XLS の取り込みは `npm run import:fetch` → `npm run import:mlit` の順に実行する（`data/raw/` はローカルキャッシュのため Git 管理外）
- 一覧ページは件数が多い（1,200 件超）ため 30 件ずつのページングを行う（`src/lib/paginate.ts`）
- 都道府県別の件数表示には `src/lib/countByPrefecture.ts` を使う
- 国土交通省データの「県名」「所在地」列は元から都道府県・市区町村に分かれているため、正規化では型の検証（`Prefecture` への絞り込み）が主な処理になる
- `id` は都道府県名と駅名から生成する安定したハッシュ値とする。駅名が変わると `id` も変わるため、データ更新時の差分検出（後述）では `id` ではなく都道府県＋市区町村＋登録回をキーに前回データと突き合わせる
- 緯度経度・連絡会の詳細 URL は国土交通省データのみでは取得できないため、正規化時点では `null` のままにする
- 連絡会データの取得は `npm run import:michinoeki:fetch`、国土交通省データへの詳細 URL 補完は `npm run import:michinoeki:merge` で行う（`src/import/matchMichiNoEkiStations.ts`）。突き合わせは都道府県＋正規化した駅名（全角英数字の半角化、空白の除去、中点「・／･」とかっこ「「」／『』」の表記揺れの統一）の一致で行い、一致しなかった駅はログに出力し `associationSourceUrls` は空配列のままにする
- 名前の自動マッチングでは検出できない名称変更・改称（例: 国土交通省データの登録時の名称と、連絡会サイト上の現在の名称が大きく異なるケース）は、登録回・登録年月・市区町村の一致や連絡会サイトのお知らせ欄の記載を根拠に手動紐付けで補う。確証が薄い組み合わせ（地名の一致のみなど）は無理に紐付けず未紐付けのままにする
- 手動紐付けデータの整合性は `src/import/validateManualMichiNoEkiLinks.ts` で検証する。存在しない駅 ID や連絡会パスを参照している場合は `npm run import:michinoeki:merge` 実行時に警告を出す（駅名変更で `id` が変わった場合などに古い手動紐付けが残っていないかを検知する）
- 連絡会データは高速道路の上り線・下り線などで1駅が複数件に分かれる場合があるため、`associationSourceUrls` は配列で持つ（国土交通省:連絡会 = 1対多）。名前の自動マッチングでは検出できないこの種のケースは、`/data-check` の手動紐付け UI でチェックして補う運用とする（`src/lib/manualLinkDraft.ts` で localStorage にドラフト保存 → 「JSONでコピー」→ `data/manual/michiNoEkiManualLinks.json` に保存 → `npm run import:michinoeki:merge` を再実行。適用処理は `src/import/applyManualMichiNoEkiLinks.ts`）
- 外部リンクは `src/components/ExternalLinks.tsx` にまとめる。`associationSourceUrls` が空の駅（連絡会データと名称が一致しなかった一部の駅）はリンクの代わりに未登録メッセージを表示する
- 緯度経度の取得元は国土地理院（GSI）の住所検索 API を想定する（API キー不要、既存の「所在地」データと相性が良い）。実際の取得・登録は外部 API へのネットワークアクセスが確保できた時点で別途行う想定で、本フェーズではデータモデルと欠落検出・バリデーションの仕組みのみ用意した
- 緯度経度の欠落検出には `src/lib/coordinates.ts`（`hasCoordinates` / `findStationsWithoutCoordinates` / `isValidCoordinate`）を使う。現在は実データの全件が `latitude`/`longitude` ともに `null`
- 詳細ページの位置情報欄（`src/components/StationCoordinates.tsx`）は緯度経度が `null` の場合「未登録」と表示し、地図データがなくても詳細ページが壊れないようにする
- 国土地理院（GSI）の住所検索 API（`https://msearch.gsi.go.jp/address-search/AddressSearch`）で実際に座標取得を試したところ、国土交通省データの「所在地」は市区町村単位までしかなく、同一市区町村に複数駅ある場合（全体の約半数、235市区町村・615駅）は同じ座標になってしまうことが分かった。地図 MVP（フェーズ 11）に進む前に、連絡会サイトの詳細ページ（番地まで含む住所が載っている）から正確な住所を取得する作業が必要（フェーズ 11 着手時に再検討）
- データ更新時の差分検出は `src/import/diffMlitStations.ts`（`diffMlitStations`）で行う。前回コミット済みの `mlitStations.json` と今回パースした結果を都道府県＋市区町村＋登録回で突き合わせ、新規登録・廃止・名称変更・所在地変更に分類して `src/data/generated/mlitStationsDiff.json` に保存する。`npm run import:mlit` 実行時に自動生成される
- 取得日時とデータ件数は `src/lib/importMeta.ts`（`ImportMeta` 型）で表し、`src/data/generated/importMeta.json` に保存する。`npm run import:mlit` で国土交通省データ分、`npm run import:michinoeki:merge` で連絡会データ分（取得日時は `data/raw/michinoeki-stations.json` のファイル更新日時を利用）を更新する
- データ取得状況・差分レポートは `/data-check` の `src/components/ImportStatusReport.tsx` にまとめて表示する
- 訪問済み状態は `src/lib/visitStorage.ts` で localStorage（キー: `mcnek:visitRecords`）に保存する。データ形式は既存の `VisitRecord` 型を使い、`memo` は本フェーズでは未使用（`null` 固定）
- 一覧の「行った道の駅だけ表示」チェックボックスのフィルタリングには `filterByVisited` を使う
- 訪問数の集計（都道府県別件数、全体の訪問数・達成率）には `src/lib/visitStats.ts`（`countVisitedByPrefecture` / `getOverallVisitStats`）を使う。専用ページは `/visited`（`src/pages/VisitedStationsPage.tsx`）で、一覧ページから「行った道の駅を見る」リンクで遷移できる
- 地図上での訪問済みピンの区別は、フェーズ 11（地図 MVP）で地図表示そのものが実装されてから対応する

## 次にやること

1. フェーズ 0 のプロジェクト土台から始める
2. 1 フェーズごとに PR を作る
3. 完了したフェーズのチェックボックスを README で更新する
4. フェーズの途中で大きくなりすぎたら、さらに小さいフェーズへ分割する
